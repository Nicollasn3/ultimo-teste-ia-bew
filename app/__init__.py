"""
Package raiz do projeto (código local).

Obs.: mantemos os módulos locais sob `app.*` para evitar colisão com o pacote externo `agents`
(do `openai-agents`), que também usa o nome de topo `agents`.
"""

